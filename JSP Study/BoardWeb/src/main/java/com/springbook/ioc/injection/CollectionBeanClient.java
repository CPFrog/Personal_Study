package com.springbook.ioc.injection;

import java.util.*;
import org.springframework.context.support.AbstractApplicationContext;
import org.springframework.context.support.GenericXmlApplicationContext;
import com.springbook.ioc.injection.CollectionBean;

public class CollectionBeanClient {
	/*
	 * public static void main(String[] args) { AbstractApplicationContext
	 * factory=new GenericXmlApplicationContext("applicationContext.xml");
	 * 
	 * CollectionBean bean=(CollectionBean) factory.getBean("collectionBean");
	 * Properties addressList=bean.getAddressList(); for(String address:addressList)
	 * //타이핑이 목적이므로 실행여부 신경 안씀. System.out.println(address); factory.close(); }
	 */
}
